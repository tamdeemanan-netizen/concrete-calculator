import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.concretecalculator',
  appName: 'Concrete Calculator',
  webDir: 'dist/public'
};

export default config;
